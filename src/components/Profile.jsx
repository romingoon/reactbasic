const Profile = ({session, logout}) =>{
     const logout = evt =>{

     }
     return (
        <>
        <div> User ID : {session.loginUser?.name}</div>
        <button> </button>
        </>
     )

}

export default Profile;